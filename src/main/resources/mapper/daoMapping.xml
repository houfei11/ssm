<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.imooc.dao.IPersonal">
    <select id="selectPersonal" parameterType="com.imooc.entity.Personal">
        SELECT * FROM personal where stuId = #{stuId}
    </select>
</mapper>